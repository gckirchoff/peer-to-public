<p><slot /></p>

<style lang="scss">
	p {
		font-size: var(--font-20);
		// font-weight: var(--font-weight-regular);
		line-height: 1.5;
		font-family: var(--font-sans);
		color: var(--clr-txt);
	}
</style>
